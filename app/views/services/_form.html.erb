	
		<div class="prepend-1">
		<% if @categories.empty? %>
				<%= link_to "Catgory", new_category_path %>
		<% else %>
			<%= f.label :category, "Category:" %>
			<%= f.select(:category_id, @categories.collect	{ |c| [c.name, c.id] }) %>
		<% end -%>
		</div>
		
		<div class="prepend-1">
	  <label class="desc" for="service_name">Name:
	  	<span class="req">*</span>
	  </label>
		<%= f.text_field :name %>
		</div>
		
		<div class="prepend-1">
			<label class="desc" for="service_start_price">Start Price:</label>
			<%= f.text_field :start_price, :size => 3 %>
			<label class="desc" for="service_end_price">End Price:</label>
			<%= f.text_field :end_price, :size => 3 %>
		</div>	