<h1 class="span-8 prepend-1">Add a new service</h1>
<% form_for @service do |f| -%>
	<%= render :partial => 'form', :locals => {:f => f }%>
	<div class="prepend-1">
		<%= f.submit "Add Service" %>
		<%= link_to "go back to services", services_path %>
	</div>
<% end -%>

