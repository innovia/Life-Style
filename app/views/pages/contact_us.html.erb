<div class="prepend-1">
<% @bkg = @page.background_picture.url(:bkg) %>
<div id="display_content" class="contact_us">
	<h2 id="title">Contact us</h2>
	<% if @page.body.nil? %>
		<h2>Coming soon...</h2>
	<%- else -%>
	<%= @page.body  %>
	<% end -%>
</div> <!-- /editable content-->

<% if logged_in? %>
	<%= yield(:yui_editor) %>
<% end -%>
</div>